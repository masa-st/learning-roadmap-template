# VS Code でこのリポジトリを開くときのセットアップ

このリポジトリの Markdown は **GitHub 上での表示を正**としています。
そのため数式の書き方が GitHub 仕様に寄せてあり、**VS Code の標準プレビューでは数式の一部が
そのままソース表示になります**。以下のセットアップで解消できます。

## なぜ設定が必要か

GitHub のディスプレイ数式には 2 通りの書き方がありますが、

- `$$ ... $$` … 独立行に置いて中に LaTeX の改行 `\\` を含めると、Markdown 側に
  バックスラッシュを食われて壊れる（`\\\\` と 4 本書く必要が出る）
- math フェンス（バッククォート 3 つ + `math`）… 中身が verbatim で渡されるので壊れない。
  GitHub 公式が `$$` の代替として案内している構文

という違いがあるため、このリポジトリでは**ディスプレイ数式を全て math フェンスで書いています**。

一方 VS Code の標準プレビューは `$...$` と `$$...$$` にしか対応しておらず、math フェンスは
非対応です（要望は microsoft/vscode の issue #201582 として出ましたが、duplicate 扱いで
クローズされ、実装されていません）。このため標準プレビューでは math フェンスが
ただのコードブロックとして表示されます。

## セットアップ手順

### 1. 拡張機能を入れる

**Markdown Preview Enhanced**（拡張 ID: `shd101wyy.markdown-preview-enhanced`）を入れます。

このリポジトリには `.vscode/extensions.json` を置いてあるので、クローンして VS Code で開くと
「この ワークスペース には拡張機能の推奨事項があります」という通知が出ます。
そこから入れるのが一番早いです。出ない場合は拡張機能ビューで `@recommended` を検索。

Markdown Preview Enhanced は `$...$` / `$$...$$` に加えて **math フェンスにも対応**しています。

### 2. プレビューの開き方

コマンドパレット（`Ctrl+Shift+P` / `Cmd+Shift+P`）から

```text
Markdown Preview Enhanced: Open Preview to the Side
```

を実行します。

**注意**: この拡張は標準プレビューを置き換えるのではなく、独自のプレビューを追加します。
`Ctrl+Shift+V` の標準プレビューは今までどおり math フェンスを描画しないので、
数式を確認したいときは必ず上のコマンド（またはそれに割り当てたキー）を使ってください。

### 3. 設定（適用済み）

`.vscode/settings.json` に以下を入れてあるので、追加の操作は不要です。

```json
"markdown-preview-enhanced.mathRenderingOption": "MathJax"
```

Markdown Preview Enhanced の既定値は KaTeX ですが、GitHub は MathJax を使っています。
KaTeX は MathJax より高速な代わりに対応していないコマンドがあるため、
**MathJax に揃えておくとプレビューと GitHub の表示差がなくなります**。

## 数式を書くときの注意

書き方の詳細ルールは [`CLAUDE.md`](./CLAUDE.md) の
「Markdown に数式を書くときのルール」にまとまっています。特に間違えやすいのは以下です。

- ディスプレイ数式は math フェンス。`$$` は使わない。
- インライン数式は `$...$`。開き `$` の直後・閉じ `$` の直前にスペースを入れない。
- **全角文字と `$` の間には半角スペースを入れる**（`変数$x$の値` は表示されない）。
- 強調記号 `**` とインライン数式を直接隣接させない。
- `` $`...`$ ``（バッククォート併用形）は使わない。
  GitHub では有効ですが、Markdown Preview Enhanced ではインラインコード扱いになり、
  両対応にならないためです。

## VS Code で開いたセッションの扱い（任意の運用）

このテンプレートは、**「スマホの Claude アプリはスキマ時間、VS Code は座ってじっくり
作業できるとき」** という使い分けを前提にした仕組みを同梱しています。VS Code の Claude Code
拡張から開いたセッションだけ、次の 3 点が変わります。

1. **集中作業モードの文脈が注入される** — SessionStart フック
   `.claude/hooks/vscode-focus-mode.sh` が「このセッションは腰を据えた集中学習として進める」
   という指示を Claude に渡します。スキマ時間前提の進め方（「概念だけ説明して、コードは後で」）を
   やめ、演習やコードもその場で一緒に進めるようになります。宿題の仕組みは残りますが、
   「スキマ時間だから」という理由での宿題化はしません。
2. **未 push チェックが働かない** — Stop フック `check-uncommitted.sh` は VS Code では何もしません。
   ファイルを自分でも編集しながら何度もセッションを止める使い方で、毎回止められないようにするためです。
3. **デフォルトブランチへの自動マージが働かない** — Stop フック `auto-merge-to-default.sh` も
   VS Code では何もしません。commit・push まではセッション終了時に通常どおり行われますが、
   マージは自分で行う（または Claude に頼む）必要があります。Claude はセッション終了時に
   その旨を一言添えるようになっています。

判定はいずれも環境変数 `CLAUDE_CODE_ENTRYPOINT` が `claude-vscode` かどうかで行っています。
ターミナルの Claude Code CLI、web、スマホアプリからのセッションは対象外で、従来どおり
動きます。

### この運用が合わない場合

VS Code を主な作業環境にしていて、VS Code でも自動マージや未 push チェックを効かせたい場合は、
次の 3 か所を揃えて外してください（1 つだけ外すと、片方だけ効く中途半端な状態になります）。

- `.claude/settings.json` の `SessionStart` の項目を削除する
- `.claude/hooks/check-uncommitted.sh` 冒頭の `CLAUDE_CODE_ENTRYPOINT` を見ている `if` ブロックを削除する
- `.claude/hooks/auto-merge-to-default.sh` 冒頭の同じ `if` ブロックを削除する

`vscode-focus-mode.sh` 自体は残しておいても、`settings.json` から外れていれば呼ばれません。

## この方針のトレードオフ

拡張機能を入れた環境でしか VS Code 側の表示が揃わない、という環境依存の解決です。
拡張を入れていない VS Code や他のエディタでは、ディスプレイ数式が LaTeX ソースのまま
等幅表示になります（読めなくはない）。

GitHub 上の表示は拡張の有無に関係なく常に正しく出るので、
**迷ったら GitHub のプレビューを正とみなしてください。**
