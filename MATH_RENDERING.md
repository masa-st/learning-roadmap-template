# 会話での数式レンダリングの実測経緯

このファイルは、`CLAUDE.md`「会話中に数式を書くときのルール」の各制約が **なぜそうなっているか** を
記録した実測ログ。iPhone の Claude アプリで、どの書き方が崩れる／直るかを検体を出して切り分けた
結果をまとめてある。実行時に従うべき **結論そのもの** は CLAUDE.md 側にあり、このファイルは
ルールを変える・疑うとき、または新しい崩れ方を調べるときに参照する資料。肥大化を避けるため、
経緯は本編から分離してここに置いている。

実測はすべてテンプレート作成元のリポジトリで、iPhone の Claude アプリを使って行ったもの。
各項の「発端」に出てくる分野名・章番号はその作成元での学習セッションを指しており、
このテンプレートの分野構成とは対応しない。自分の環境で新しい崩れ方・直り方を見つけたら、
同じ書式（発端 → 切り分けの検体と結果 → 対処 → 未検証の条件）でここに追記する。

ここに書いてあるのはすべて **アプリ（会話）上の表示** の話。ファイル書き出し（GitHub の MathJax）の
制約は CLAUDE.md「Markdown に数式を書くときのルール」が正で、別物。

## 経緯: `$$` で行列を書くと表示が崩れる

**2026-08 追記: この事象は現在のアプリでは再現しなくなった（アプリのアップデートで改善したとみられる）。**
2026-08 に、単純な `pmatrix`、1セルに記号2個（`2x` `3y` 等）、ヤコビ行列・ヘッセ行列、
生の偏微分をセルに置いた行列（`\partial_x f` 等）、セル内で微分演算子×関数の積を並べた行列、
分子が関数の積の分数を並べた行列など、下記の重複が起きるはずのパターンを一通り `$$` で出して
実測したが、いずれも末尾記号の重複は起きなかった。したがって **`$$` で行列を書いても崩れない**。
ただしここで「行列」と言っているのは `pmatrix` 系のことで、**`array` 環境は別で、こちらは崩れる**
（下の「`array` 環境は崩れる」を参照）。
これを受けて、会話でのディスプレイ数式は **レンダリング優先（`$$` を第一候補）** に方針変更した
（CLAUDE.md の「結論」参照）。math フェンスは、レンダリングすると崩れる数式の逃げ道として使う。
以下は当時（2026-07）の記録として残す。

当初は「会話では `$$ ... $$` を使う」という運用にしていたが、行列で表示が崩れることが分かり、
math フェンス運用に戻した時期があった。切り分けの結果、崩れるのは
**行列（`pmatrix` 等）の1セルに記号を2個以上並べたとき** で、その **セルの最後の記号が重複して
表示される**（2026-07 時点の iPhone アプリでの実測）。

| 書いたもの | 表示 |
|---|---|
| セル内の `xy` | `xyy` |
| セル内の `\partial f_1` | `\partial f_1 f_1` |
| セル内の `ab` | `abb` |

- 行列そのもの、`\frac` 単体、添字単体は問題ない。崩れるのは組み合わせのとき。
  行列の **外** なら `\frac{\partial f_1}{\partial x_1}` も崩れない。
- グループ化 `{...}` で囲んでも回避できない。
- `$$` でどうしても行列を書くなら、セルを記号1個に保つ（`f_1` や `\partial_1 f_1` は無事）。
  ヤコビ行列・ヘッセ行列なら `\frac{\partial f_i}{\partial x_j}` ではなく
  `\partial_j f_i` 記法にすれば崩れない。
- 原因の深追いはしない（アプリ内部の問題で外からは特定できず、学習時間を削るだけ）。
  表示が崩れたらユーザーが指摘するので、その都度書き方を変えれば足りる。

## 経緯: 表のセルに数式を書くと消える

ユーザーから複数回（少なくとも 2 回）指摘のあった再発事象。指数型分布族の説明で
「登場人物は4つです」として下のような表を出したところ、アプリ上では
**`$\theta$` などを入れたセルが空欄になり、文中の `$\theta$` も脱落して
「 $\theta$ に依存しない」が「に依存しない」と表示された**（2026-07 時点の実測）。

**2026-08 追記: 表のセル内の数式が消える現象は現在も再現する（要注意・ルール維持）。
一方、併発していた「文中の `$\theta$` も脱落する」現象は解消し、文中の数式は正常に表示される
ようになった。** 2026-08 に表のセルに `$\theta$` 等を入れた表とその直後の文を出して実測した
結果、**表内のセルは空欄のまま** だったが、文中の「分配関数は $\theta$ に依存しますが、
基底測度は $\theta$ に依存しません」は $\theta$ を含めて正しく表示された。
したがって **「表のセルに数式を書かない」というルールは引き続き有効**（下記の対処も維持）。
影響が文中まで及ぶという当時の記述だけが現状に合わなくなった。

| 書いたもの | 表示（2026-08 実測） |
|---|---|
| セルが `$\theta$` だけ | 空欄になる（現在も再現） |
| 表の直後の文中の `$\theta$` | 正常に表示される（当時の脱落は解消） |

- 行列崩れ（上記）とは別の事象。あちらは記号が重複、こちらは数式そのものが消える。
- 再発の原因は、ファイル用ルール 6（表のセル内に数式を書いてよい）を会話に持ち込んだこと。
  会話用ルールとファイル用ルールは別物であり、**表については正反対** だと覚えておく。
- 記号と役割の対応表のような内容は、会話では箇条書きに直す。
- 避けるべきなのは「セル内の数式」であって、表そのものではない。アプリの表は横スクロールできる
  ので、幅が広いこと自体は問題にならない（数式を含まない表は会話でも普通に使ってよい）。

## 経緯: `\dots` を含む数式が崩れる（旧題「カンマ区切りの列挙が原因」は誤診）

犯人は `\dots` コマンドそのもの。`\dots` を含む数式は **インライン・ディスプレイ（`$$`）を問わず**、
前後がカンマ列（`0,1,\dots,n`）でも演算子列（`a_1+\dots+a_n`）でも一様に崩れ、生ソースが表示される。
逆に `\dots` を含まなければ、カンマ区切りの列挙（`\{0,1\}` など）を含んでいても崩れない。
`\dots` は文脈依存で `\ldots`/`\cdots` を選ぶマクロで、アプリの MathJax がこれを解釈できないためと
みられる（`\ldots`・`\cdots` は TeX の基本コマンドなので常に通る）。

- **対処: `\dots` を使わず、カンマ区切りの列は `\ldots` 、演算子で挟む列は `\cdots` に置き換える。**
  どちらもインライン・ディスプレイのどちらでも安全。列挙を数式の外に出す必要はなく、
  `$x_1,\ldots,x_n$` のようにそのままインラインで書ける。
- これはアプリ（会話）固有の事象。ファイル書き出し（GitHub の MathJax）では `\dots` も問題なく
  レンダリングされるので、ファイル用ルールには影響しない。

**経緯（誤診の記録）:** 2026-07 に情報幾何学のセッション（凸解析・凸包）で `$x_1, \dots, x_k$`
が崩れたのを最初に観測し、当初は「カンマ区切りの列挙が原因」と診断して、列挙を数式の外に出す・
ディスプレイなら安全、という回避策をとっていた。2026-08-17 に検体を出して切り分け直したところ、
真因は `\dots` 単独であり、カンマもディスプレイか否かも無関係と判明したため、上記に置き換えた。

## 経緯: 長い数式が切れるのはインラインのときだけ

「横に長い数式は画面幅で切れる」というルールを当初は数式全般に適用していたが、
2026-07 に長い数式を A〜D の4パターン出して実測したところ、切れるのはインラインだけだった。

- A: math フェンスに改行なしの1行 → **横スクロールで最後まで読める**
- B: math フェンスに `aligned` で分割 → 問題なし
- C: 長いインライン数式を地の文に埋め込み → **右が切れ、横スクロールもできない**
- D: `$$ ... $$` の長いディスプレイ数式（行列なし） → 問題なし

したがって対策は「式を短く割る」ことではなく「インラインに置かない」こと。
ディスプレイに出せば長さは気にしなくてよい。

## 経緯: `\dfrac` と `\frac` はどちらでも同じ（実測）

かつて CLAUDE.md 会話用ルールに「`\dfrac` や `\\[6pt]` のような高さを広げる指定は避け、
`\frac` で十分」という項があったが、`\dfrac` が崩れる・高さが問題になるという実測記録はなく
（このファイルにも記述がなく、CLAUDE.md 側の当該項だけが唯一「◯年◯月実測」の出典を欠いていた）、
`\\[6pt]` と一括りにした予防的な作法にとどまっていた。2026-08-24 に `\frac` と `\dfrac` を
対にした検体（インライン、単純ディスプレイ、`\begin{cases}` の場合分け、入れ子分数の4種）を
出して実測したところ、**いずれのパターンでも生ソース化せず、両者に表示上の差もなかった**
（インラインの行の高さの差も気にならない程度）。したがって `\dfrac`／`\frac` はどちらを使ってもよい。

- これを受けて CLAUDE.md の `\dfrac`／`\frac` への言及は削除した（実行時に守るべき差がなく、
  本編を短く保つ方針のため）。

## 経緯: `\\[6pt]` のような行間指定は崩れる（`[6pt]` が文字列として表示される）

`\dfrac` と対で「`\dfrac` や `\\[6pt]` のような高さを広げる指定は避け、`\frac` で十分」という
予防的な作法にとどまっていた項のうち、`\dfrac` 側は 2026-08-24 に実測して崩れないと確認し削除した
（上記）。`\\[6pt]` の行間指定は「`\dfrac` とは別件」として実測せずに CLAUDE.md に残していたので、
同じ 2026-08-24 に検証した。

**誤診と訂正の記録:** 当初、同じ `aligned`（3本の乖離度の定義）を `\\[6pt]` あり・なしの2版で `$$`
ディスプレイに出して比較し、ユーザーが「どちらでも良い（差が気にならない）」と回答したため、
いったん「崩れない → ルール削除」と判断して CLAUDE.md の当該項を削除した。しかしユーザーが
**レンダリング結果を拡大して見直したところ、`\\[6pt]` の側で崩れが起きていた**（画面写真で確認）。
最初の「どちらでも良い」はざっと見での見落としだった。

**症状:** `$$ ... $$` の `aligned` 内で行区切りを `\\[6pt]` と書くと、`\\` は改行として効くが、
続く `[6pt]`（間隔指定の引数）が**間隔として解釈されず、`[6pt]` という文字列がそのまま次の行の
先頭に表示される**（`\\[6pt]D_E(P:Q) = ...` のように見える）。アプリの MathJax が `\\` 直後の
オプション引数 `[...]` をパースできないためとみられる。生ソース化や消失ではなく「引数が地の文に漏れる」
タイプの崩れ。

- **対処: `\\` の直後に `[6pt]` のような間隔指定を付けない。行区切りは `\\` だけにする。**
  つまり **元の予防ルール（`\\[6pt]` を避ける）は正しかった**。今回の実測で裏づけが得られたので、
  いったん削除した CLAUDE.md の項を復活させた（今度は実測の出典つき）。
- `\dfrac` の方は実測で崩れないと確認済みなので削除のまま。`\dfrac`（無害）と `\\[6pt]`（有害）は
  もともと一括りにされていたが、実測で明暗が分かれた。
- この教訓: レンダリングの可否は「ざっと見て崩れていない」では足りず、引数の漏れのような細部は
  拡大して確認する必要がある。「どちらでも良い」という回答が来ても、崩れやすい記法は念のため
  細部を問う。

## 経緯: `\*`（エスケープしたアスタリスク）で数式が崩れる

犯人は数式内の `\*`（バックスラッシュ＋アスタリスク）。markdown では `*` が強調記号なので、
その反射で数式内でも `\*` とエスケープして書くと、`\dots` と同様に **数式が丸ごと生ソースになる**
（インライン・`$$` ディスプレイの別を問わず）。`\*` は MathJax の標準マクロにない指定のため
解釈に失敗するとみられる。

2026-08-24、第2種過誤の検定問題の解説中、境界を `\mu^\*` と書いた箇所（インラインの
`$\mu^\* = c = \ldots$`、および `\begin{cases}` を含む `$$` ディスプレイの両方）が生ソース化した。
当初は「`$$` の中の `\begin{cases}` と改行 `\\` が原因」と誤診したが、崩れた式はすべて `\mu^\*` を
含んでおり、星を使わず境界を `c` と書き換えた（`\*` を除いた）ところ、`\begin{cases}` を含む `$$`
ディスプレイも問題なくレンダリングされた。したがって原因は `$$` でも `\begin{cases}` でもなく
`\*` 単独。

- **対処: 数式内で `*` をエスケープしない。** 上付きの星などが必要なら素の `*`（`x^*`）か
  TeX の基本コマンド `\ast` を使う（`\ldots`/`\cdots` と同じく標準コマンドなので安全なはず。
  ただし素の `*`・`\ast` 自体は今回直接は実測しておらず、確実なのは「`\*` は崩れる」まで）。
- これはアプリ（会話）固有の事象。ファイル書き出し用ルール（CLAUDE.md「Markdown に数式を
  書くときのルール」2）は逆に、GitHub で壊れる場合に `*` を `\*` とエスケープしてよいとしている。
  会話とファイルで正反対なので混同しない。

## 経緯: 引用ブロック（`>`）の中の `$$` は `\\` を食われて崩れる

犯人は **引用ブロック（markdown の `>` 記法）の中に置いた、LaTeX の改行 `\\` を含む `$$`**。
引用ブロックは行頭マーカーを行ごとに剥がす処理をするため、その過程で `$$` の中の `\\` の
バックスラッシュを食う。食われた後の LaTeX が

- **不正になる場合**（`cases`・行列など `\\` が行区切りに必須の環境）は、式全体が生ソースに崩れる。
- **有効なままの場合**（`aligned`・単純な `a\\b` など、`\\` を抜いても1行として通る式）は、
  表示はされるが**改行が消える**（意図と違う1行表示になる）。

2026-08-24、最強力検定の講義でネイマン・ピアソンの補題の定理文を `>` の引用ブロックで囲み、
その中の `$$\varphi^*(x)=\begin{cases}\ldots\end{cases}$$` が生ソース化した。当初は素のアスタリスク
`*` や不等号 `<`,`>` を疑って単独の検体を多数出したがいずれも再現せず（＝それらは無実）、
一度は一過性かと思われた。決め手はユーザーがスクショ内の**引用の左罫線（グレーの縦線）**に
気づいたことで、犯人が引用ブロックだと判明。引用の外に出した同じ式（`cases`）は正常表示された。

**実測した境界（限定的な確認）:**

- 崩れる: 引用 `>` の中の `\\` 入り `$$`。`cases` は生ソース化、`aligned` は改行落ち。
- 安全: 左端・前後空行のトップレベルの `$$`（`cases` も可）／**箇条書き・番号付きリストの中**の
  `$$`（`cases` も可。リストは引用と違い `\\` を食わない）／引用の中でも `\\` を含まない
  **単一行** `$$`／引用の中の**インライン** `$...$`。

**対処:** 複数行のディスプレイ数式（`cases`・`aligned`・行列・手動 `\\`）は引用の中に入れず、
引用の外（トップレベル）に出す。定理なども `>` で囲まず太字見出し＋普通の段落で組む。

**注意:** この境界の確認は**さらっとしたもの**で、未検証の条件が残る（引用のネスト、生のインデント
のみ、`\\` を `\newline` 等に替える回避が効くか、リストのネストや深い階層など）。新しい崩れ方を
見つけたら追記する。なお、これはファイル書き出しルール1（`$$` を使わず math フェンス。理由の一つが
「箇条書きの中やインデント下で `\\` を食われて壊れる」）の会話版の裏づけでもある。会話では
`$$` レンダリングを優先するが、引用内の複数行だけは例外的に避ける。

## 経緯: `\overset` / `\underset` / `\stackrel` と拡張矢印 `\xrightarrow` は崩れる（積む系一般ではない）

犯人は「**関係記号（や任意の土台）の上下に、任意の数式ラベルを乗せて新しい関係を作る**」系の
コマンド。具体的には `\overset` `\underset` `\stackrel`、および拡張矢印 `\xrightarrow` `\xleftarrow`。
これらはアプリでレンダリングされず、数式が丸ごと生ソースになる（インライン・`$$` ディスプレイの別を
問わず）。アプリのレンダラがこれらのマクロだけ未実装とみられる。

2026-08-28、⑤仮説検定論の尤度比検定の講義中、分布収束を `\Lambda \xrightarrow{d} \chi^2_r` と書いた
ディスプレイ数式が生ソース化したのが発端。ユーザーの依頼で表示テストを2弾に分けて切り分けた。

**第1弾（`\overset` が犯人か、対照は無実か）:**

| テスト | 内容 | 結果 |
|---|---|---|
| `\overset{d}{\to}`（インライン） | 本命 | **崩れた** |
| `\overset{d}{\longrightarrow}`（`$$`） | 本命 | **崩れた** |
| 素の矢印 `\to` / `\longrightarrow` | 対照 | 正常 |
| `\text{Exp}(\theta)` | 対照 | 正常 |
| `\ldots` | 対照 | 正常 |
| `\sim` | 対照 | 正常 |
| `\stackrel{d}{\to}` | 代替案 | **崩れた** |

`\overset` が崩れ、`\text`・`\ldots`・`\sim`・素の矢印は無実と確定。代替に期待した `\stackrel`
（`\overset` と同機能の別名）も崩れたため、当初は「積む系は全滅か」と広く疑った。

**第2弾（積む系一般か、`\overset` 一族限定か）:**

| テスト | コマンド | 結果 |
|---|---|---|
| `\underset{n\to\infty}{\lim}` | 下に積む | **崩れた** |
| `\substack`（和の2段添字） | 2段積み | 正常 |
| `\atop` | 素朴な2段積み | 正常 |
| `\overbrace` / `\underbrace` | 波括弧 | 正常 |
| `\overline` / `\underline` | 横線 | 正常 |
| 標準の `\sum` / `\lim` の上下添字 | 対照 | 正常 |
| アクセント `\hat{}` `\bar{}` `\tilde{}` | 対照 | 正常 |

第2弾で「積む系全滅」の広い予想は**外れ**、犯人は **`\overset` / `\underset` / `\stackrel` の3つ**
（＋拡張矢印）に絞られた。同じ「上下に乗せる」でも、固定の飾り（波括弧・横線）を乗せる `\overbrace`
`\overline`、分数的な2段積み `\atop` `\substack`、アクセント、標準の `\sum`/`\lim` 添字はすべて無事。
区別は「**可変ラベルを関係の上下に貼って新しい関係を作る**」かどうか。

- **対処: `\overset` `\underset` `\stackrel` `\xrightarrow` `\xleftarrow` を使わない。** 上付き・下付きの
  注記つき矢印・関係は数式内で作らず、注記を数式の外の地の文に逃がす（分布収束は
  「$\to\chi^2_r$（分布収束）」、iid は「$\sim\ldots$（iid）」）。式の一部を囲う注記が要るときは
  `\underbrace{\ldots}_{\ldots}` が使える（第2弾で無事と確認済み）。
- これはアプリ（会話）固有の事象。ファイル書き出し（GitHub の MathJax）ではこれらも通常はレンダリング
  されるので、ファイル用ルールには影響しない。
- 未検証: `\mathop\limits` の手組み、`\atop` で矢印上にラベルを自作した場合の見栄え、`\xhookrightarrow`
  など拡張矢印の他の変種。実務上は「注記は地の文・囲みは `\underbrace`」で足りるので深追いしていない。

## 経緯: 全角文字と `$` の直結は会話では崩れない（読みやすさのため②推奨）

「全角文字と `$` の間に半角スペースを入れる」ルールは、もともとファイル用ルール（GitHub の MathJax
では全角直結が「認識されない事例が報告されている」）からの持ち込みで、会話については実測がなく
「守ると読みやすい」と書かれていた。`\dfrac`・`\\[6pt]` と同じ「出典を欠いた予防的作法」の疑いが
あったため、2026-08-24 に検証した。

① スペースなし（全角直結）と ② 半角スペースありの2版を、以下の3ケースでインラインに出して比較:

| ケース | ① 例 | ② 例 |
|---|---|---|
| 全角文字が `$` に隣接 | `変数$x$の値` | `変数 $x$ の値` |
| 読点直後に数式 | `密度を、$r^2$ の関数` | `密度を、 $r^2$ の関数` |
| 全角で挟む連続 | `集合$A$の要素$a$` | `集合 $A$ の要素 $a$` |

**結果（ユーザー実測）: ① も生ソース化・脱落・崩れは起きず正常にレンダリングされた。ただし ② の方が
少し読みやすい。** すなわち会話では「崩れる問題」ではなく「読みやすさの差」だった。

- **対処: 会話では ① もダメではないが ② を推奨（既定は②）。** 崩れ防止ではないので必須ではない。
  CLAUDE.md の会話用ルールを「崩れる」から「読みやすさのため推奨」のニュアンスに書き換えた。
- ファイル書き出し（GitHub）では全角直結が認識されない事例が報告されており、ファイル用ルール 3 では
  引き続き **必須**。会話は「推奨」、ファイルは「必須」で強さが異なる。
- なお「数式内は半角のみ・日本語は `\text{...}`」は崩れ防止ではなく LaTeX の作法であり、これは会話・
  ファイルとも維持（今回の緩和は全角と `$` の間のスペースだけが対象）。

## 経緯: 直前に空行がない `$$` は横スクロールできず右が切れる

「長い数式はディスプレイ `$$` に出せば横スクロールできるので幅は問題ない」（上記「長い数式が切れるのは
インラインのときだけ」）には条件があると判明。**`$$` の直前に空行がないと横スクロールが効かず、
長い1行の式の右が切れて読めなくなる。**

2026-09-07、①確認テスト Q9 の解説で「包除原理:」という行の**直後の行**に `$$` を直結させたところ、
長い1行の `$$` の右が切れ、横スクロールもできなかった。ユーザーの指摘で、コロンの有無・空行の有無を
2×2で振り、同じ長い式を出して切り分けた。

| テスト | 直前行 | `$$` 直前の空行 | 結果 |
|---|---|---|---|
| A | コロンで終わる | なし | 切れる（スクロール不可） |
| B | コロンで終わる | あり | 正常（スクロール可） |
| C | ふつうの文 | なし | 切れる |
| D | ふつうの文 | あり | 正常 |

**犯人は「空行なし」でコロンは無関係。** 空行がないと `$$` が直前の段落に吸収されて独立ブロック扱いに
ならず、`overflow-x` の付いたブロックにならないため横スクロールできない、とみられる。

- **対処: ディスプレイ `$$` は直前（できれば直後も）に空行を入れて独立ブロックにする。** そうすれば
  長くても横スクロールで読める。空行を空けられない文脈（箇条書きの直後に続けたい等）では、
  `aligned` で複数行に折って幅を詰める。
- これはアプリ（会話）固有の表示の話。ファイル書き出し（GitHub）はそもそも math フェンスを使うので
  別問題。

## 経緯: `\middle` は崩れる（縦棒限定ではなくコマンド自体が未対応）

犯人は `\middle`。`\left(` と `\right)` の間で区切り記号を伸縮させるこのコマンドは、アプリで
レンダリングされず数式が丸ごと生ソースになる。**渡す区切り記号によらず崩れる**ので、
「縦棒が悪い」のではなく `\middle` というコマンド自体が未実装とみられる。

2026-09-14、統計応用③疫学・臨床試験の概観のなかで、症例数設計の検出力を
`\Pr\left(\left|\ldots\right|>z_{\alpha/2}\;\middle|\;\Delta\right)` と書いた `$$` が生ソース化した
のが発端。`$$` の直前に空行はあり（上記の空行ルールは満たしていた）、`\left|` による絶対値も
同じ式に同居していたため、まず「縦棒系が全般に駄目なのでは」と広く疑って切り分けた。

**第1弾（縦棒まわりの候補と対照）:**

| テスト | 内容 | 結果 |
|---|---|---|
| `\lvert Z \rvert`（絶対値の別綴り） | 代替案 | 正常 |
| `\Pr(A \mid B)`（`\mid`） | 代替案 | 正常 |
| 縦棒を一切使わない `\Phi(\ldots)` | 対照 | 正常 |
| `\left\|Z\right\|`（伸縮する絶対値） | 本命候補 | 正常 |
| `\Pr\left(Z>z \;\middle\| \Delta\right)` | 本命候補 | **崩れた** |

`\left|` / `\right|` が無事だったことで「縦棒系全般」の予想は外れ、犯人は `\middle` に絞られた。

**第2弾（`\middle` の何が駄目か）:**

| テスト | コマンド | 結果 |
|---|---|---|
| `\;` 単独（`\Pr\left(A \;\cap\; B\right)`） | 対照 | 正常 |
| `\Pr\left(A \middle\| B\right)`（`\;` なし） | 本命 | **崩れた** |
| `\Pr\left(A \middle\vert B\right)`（別綴り） | 本命 | **崩れた** |
| `\left(\frac{a}{b} \middle/ \frac{c}{d}\right)`（縦棒以外） | 本命 | **崩れた** |
| `\Pr\left(A \bigm\| B\right)` | 代替案 | 正常 |
| `\Pr\left(\ldots \,\Big\|\, \mu=\mu_1\right)` | 代替案 | 正常 |
| 背の高い中身に `\mid`（実用性確認） | 代替案 | 正常 |

`\;` は無実。`\middle` は **縦棒 `|`・`\vert`・スラッシュ `/` のいずれを渡しても崩れる** ので、
区切り記号の種類ではなくコマンドそのものが原因と確定した。一方、`\middle` を経由しない
サイズ指定（`\bigm|`・`\Big|`）と、素の `\mid` はすべて無事。

- **対処: `\middle` を使わない。** 条件付き確率の縦棒は `\mid` で書く。中身が背の高い分数などで
  細い縦棒では見劣りする場合は、`\bigm|` や `\Big|` で手動サイズ指定すれば伸縮相当の見た目になる
  （どちらも実測で無事）。絶対値は `\left|` / `\right|` でも `\lvert` / `\rvert` でもよい。
- `\overset` 系と同じく「アプリのレンダラが一部マクロ未実装」というパターン。ファイル書き出し
  （GitHub の MathJax）では `\middle` も通常レンダリングされるので、ファイル用ルールには影響しない。
- 未検証: `\middle\}` など他の区切り記号、`\middle` を `\big` 系と併用した場合。実務上は
  「`\mid`、必要なら `\bigm|` / `\Big|`」で足りるので深追いしていない。

## 経緯: `array` 環境は崩れる（`pmatrix` 系は無事）

犯人は `array` 環境そのもの。`\begin{array}{...} ... \end{array}` を `$$` で囲むと、アプリで
レンダリングされず数式が丸ごと生ソースになる。**列指定の縦棒や中身によらず崩れる**ので、
`\middle` と同じく「環境そのものが未実装」型の事象とみられる。

2026-09-14、統計応用③の症例対照研究の説明で $2 \times 2$ 分割表を
`\begin{array}{c|cc}` で組んだ `$$` が生ソース化したのが発端。`$$` の直前に空行はあり、
セルの中身も記号1個（`A`〜`D`）だったため、上の「行列のセルに記号2個」とは別の事象。
候補は「`array` 自体」「列指定の縦棒 `{c|cc}`」「行列内の全角 `\text{}`」「先頭の空セル」の4つで、
これを切り分けた。

| テスト | 内容 | 結果 |
|---|---|---|
| A: `\begin{array}{ccc}`（縦棒なし・半角のみ） | 対照 | **崩れた** |
| B: `\begin{array}{c\|cc}`（A に縦棒を追加） | 候補 | **崩れた** |
| C: `\begin{array}{ccc}` ＋ `\text{発症あり}`（A に全角を追加） | 候補 | **崩れた** |
| D: `\begin{pmatrix}` ＋ `\text{発症あり}` | 対照 | 正常 |

A が崩れた時点で犯人は `array` 自体に確定し、B・C の追加要素は原因ではないと分かった。
D が無事なことから、**全角の `\text{}` は行列の中でも無実**で、`pmatrix` 系は従来どおり
`$$` で使える（上の「行列を書いても崩れない」はそのまま有効）。

- **対処: `array` を使わない。**
  - 分割表・対応表のような **表組み** は、そもそも数式にせず GFM のパイプ表で書く。セル内に
    `$...$` を入れなければ消えないので（「表のセルに数式を書くと消える」参照）、 $2 \times 2$ 表の
    `A`〜`D` のような記号はインラインコードか素のアルファベットで置けば足りる。読みやすさでも
    こちらが上。
  - 行列は `pmatrix` / `bmatrix` / `vmatrix` を使う。`array` を使う理由の大半は列指定
    （`{c|cc}` の縦棒による区切り）だが、これは表組みが欲しいだけのことが多い。
  - **拡大係数行列 $[A \mid b]$ のように、行列の中に縦棒の区切りが要る場合**は、下記の
    「縦棒つき行列の代替」のとおり区切り記号の対で2つの行列を連結する。当初はここを
    math フェンス送りにしていたが、実測でレンダリングできる書き方が見つかったので撤回した。
- ファイル書き出し（GitHub の MathJax）では `array` は通常レンダリングされるので、ファイル用
  ルールには影響しない。math フェンスの中に `array` を書くのは従来どおり問題ない。
- 未検証: `subarray`、`smallmatrix`、`cases` の変種（`cases` 自体は「引用ブロックの中の `$$`」の
  切り分けで引用の外なら無事と確認済み）、`aligned` の中に `array` を入れ子にした場合。
  実務上は「表はパイプ表、行列は `pmatrix`、縦棒区切りはフェンス」で足りるので深追いしていない。

### 続き: 縦棒つき行列の代替（`array` なしで拡大係数行列を出す）

上記で `array` を封じた直後、「フェンスに逃がす前に表示できる方法を探してほしい」という
要望があったため、同じ 2026-09-14 に代替案を切り分けた。`array` と `\middle`（ともに実測で
崩れる）を経由せずに縦棒を出す手として、次の4つを試した。

| テスト | 内容 | 結果 |
|---|---|---|
| E: `\begin{matrix}` 単体 | 対照（`matrix` は生きているか） | 正常 |
| F: `\left(A\right\|\left. b\right)`（区切り記号の対で連結） | 本命 | 正常。棒が高さいっぱいに連続 |
| G: `\left(A \;\Big\vert\; b\right)`（1つの括弧に `\Big\vert` を挟む） | 代替 | 正常。ただし棒の高さは固定 |
| H: `pmatrix` に `\big\|` だけの列を作る | 代替 | 正常。ただし棒が行ごとに分断される |

**4つとも崩れなかった**ので、`array` が死んでいても縦棒つき行列は書ける。選定は見た目で行った。

- H は棒が行ごとに切れて行間に隙間が見えるため、拡大係数行列としては見劣りする。
- G の `\Big\vert` は **高さが固定** で、3行4行の行列では棒が足りず、そのつど `\Bigg` へ手で
  上げる必要がある。
- F の `\right|` は `\left(` と対になって **中身の高さに自動で合う**。3行の検体（F''）でも
  棒が全高に伸びることを確認したので、行数を問わず使える。**F を標準とする。**

**余白の調整は効かない。** F は `2|5` のように棒の両脇が詰まって見えるため、`\,` を行列のセルの
末尾に置いた版（F'）と、`\;` を `\end{matrix}` の外に置いた版（F'''・F''''）も実測したが、
どちらも見た目はほとんど変わらなかった（セル右端および `\right|` の前後で空白が吸収される
とみられる）。実用上の差がないので、**余白指定を入れない素の F を標準形とする**。

なお F' の初回検体は左側の行列の2行目を書き落としており（`1 & 2` の1行だけにしてしまった）、
左右の高さが食い違った状態で表示された。書き損じであって書式の問題ではない。**左右の行列の
行数を必ず揃える**ことは、この書き方の唯一の注意点として挙げておく。

**標準形:**

```
\left(\begin{matrix} 係数部 \end{matrix}\right|\left.\begin{matrix} 右辺 \end{matrix}\right)
```

角括弧なら `\left(` `\right)` を `\left[` `\right]` に替える。E が無事だったことから、
**崩れる行列環境は `array` だけ**で、`matrix`・`pmatrix` 系はすべて使えると考えてよい。
