# このテンプレートから自分の学習用リポジトリを作る

このドキュメントは、**テンプレートリポジトリ
[`masa-st/learning-roadmap-template`](https://github.com/masa-st/learning-roadmap-template)
から自分のリポジトリを作り、最初の学習セッションを始めるまで**の手順書です。

環境ごとの設定は別ファイルにあります。スマホの Claude アプリから使う場合は
[`MOBILE_SETUP.md`](./MOBILE_SETUP.md)、PC の VS Code でリポジトリを開く場合は
[`VSCODE_SETUP.md`](./VSCODE_SETUP.md) を参照してください。
このドキュメントからも、必要になる箇所でそれぞれを案内します。

## 1. 事前に必要なもの

- **GitHub アカウント**（無料プランで構いません）
- **Claude Code が使えるプラン** — Pro または Max（Team / Enterprise でも構いません）
- **スマホ / タブレット** または **PC**（詳細は [`README.md`](./README.md) の「必要なもの」）

## 2. テンプレートからリポジトリを作る

GitHub の **テンプレートリポジトリ** の機能を使います。ファイル・ディレクトリ構成をそのまま
引き継いだ新しいリポジトリが、自分のアカウントの下に作られます。

1. ブラウザで
   [テンプレートリポジトリのページ](https://github.com/masa-st/learning-roadmap-template)
   を開く。
2. ファイル一覧の上にある **Use this template**（このテンプレートを使用）をクリックし、
   **Create a new repository**（新しいリポジトリを作成）を選ぶ。
   - [github.com/masa-st/learning-roadmap-template/generate](https://github.com/masa-st/learning-roadmap-template/generate)
     を直接開いても同じ画面に行けます。
3. **Owner** — 作成先のアカウントを選ぶ。個人アカウントのほか、権限があれば
   Organization も選べる。
4. **Repository name** — リポジトリ名を入力する（例: `learning-roadmap`）。
   **Description** は任意。
5. **Include all branches** — チェックを入れると、テンプレート側のすべてのブランチが
   コピーされる。**このテンプレートではチェック不要**（外したままでよい）。
   既定ではデフォルトブランチだけがコピーされ、それで十分です。
6. **Public / Private** — **Private を推奨**します。学習記録には理解度や個人的なメモが
   残るためです。後から変更もできます。
7. **Create repository from template**（テンプレートからリポジトリを作成）を押す。

しばらく待つと、自分のアカウントの下に新しいリポジトリができます。**以降の作業は、
テンプレート本体ではなく、ここで作った自分のリポジトリに対して行います。**

### 2-1. Use this template ボタンが見当たらない場合

**このボタンは GitHub にサインインしているときだけ表示されます。** サインアウトした状態で
テンプレートリポジトリのページを開くと、ボタン自体が現れません。

画面右上の **Sign in** からサインインし、テンプレートリポジトリのページを開き直してください。
サインアウトのまま上の `.../generate` の URL を直接開いてもエラーになるので、
先にサインインを済ませておくのが確実です。

### 2-2. GitHub CLI で作る場合

PC で `gh` コマンドを使っているなら、ブラウザを開かずに作れます。

```bash
gh repo create <リポジトリ名> \
  --template masa-st/learning-roadmap-template \
  --private \
  --clone
```

`--clone` を付けると、作成と同時に手元へ clone されます。

## 3. fork との違い

どちらも「他人のリポジトリを元に自分のリポジトリを作る」操作ですが、性質が違います。

| | テンプレートから作成 | fork |
|---|---|---|
| コミット履歴 | 引き継がない（**コミット 1 個**から始まる） | 元リポジトリの履歴をすべて引き継ぐ |
| 想定する使い方 | 元と切り離した**新しいプロジェクト**を始める | 元リポジトリへ**変更を提案する**（PR を送る） |
| 元リポジトリとの関係 | 独立。上流を意識しなくてよい | 上流リポジトリと紐づく |

学習記録は自分だけのものでテンプレート側に還元する性質のものではないので、
**テンプレートから作成する方が適しています。**

## 4. 作ったリポジトリを Claude から開けるようにする

### スマホ / タブレットの Claude アプリから使う場合

[`MOBILE_SETUP.md`](./MOBILE_SETUP.md) の 3 章に沿って Claude GitHub App を連携します。

**Private にした場合は、Claude GitHub App 側でこのリポジトリへのアクセスを明示的に
許可する必要があります。** インストール時の画面で **Only select repositories** を選び、
今作ったリポジトリを指定してください。すでに App を入れてある場合は、
[`MOBILE_SETUP.md`](./MOBILE_SETUP.md) の 3-5 の手順で後からリポジトリを追加できます。

### PC の Claude Code CLI から使う場合

作ったリポジトリを clone して、そのディレクトリで Claude Code を起動するだけです。

VS Code でファイルを直接読み書きする場合は、先に
[`VSCODE_SETUP.md`](./VSCODE_SETUP.md) を読んでください。このリポジトリの数式は
GitHub 仕様（math フェンス）で書いてあるため、拡張機能を 1 つ入れないと
VS Code の標準プレビューでは数式がソース表示になります。

## 5. 最初の学習セッションまで

1. **見本を残すか決める** — このテンプレートには記入見本として
   [`statistics-certificate-pre1/`](./statistics-certificate-pre1/ROADMAP.md)（統計検定準1級）が
   入っています。その分野を学ぶならそのまま使えます。学ばないなら、ディレクトリごと削除し、
   `README.md` の「構成」と「分野一覧」からも該当行を消してください（消さないとリンク切れが
   残ります）。判断がつかなければ残したままでも支障ありません。
2. **最初のロードマップを作る** — セッションを開始して「○○を学びたい」と伝えると、
   `create-roadmap` スキルが学びたい分野・学ぶ動機（目指す方向性）・現在の知識レベルを
   ヒアリングし、対話でロードマップ（`<分野ディレクトリ>/ROADMAP.md`）を作ります。
   見本をそのまま使う場合、この手順は不要です。
   - 他のチャット AI（ChatGPT など）で作ったロードマップ案がある場合は、それを貼り付けて
     「これを取り込んで」と伝えれば、同じスキルがヒアリングを最小限に絞って取り込みます。
     章の粒度・到達目標の書き方・数式記法をこのリポジトリの形式に整え、既存分野と
     重複する章がないかも確認したうえで作成します。
3. **学習する** — 「①の続きから」などと伝えれば、Claude が `CLAUDE.md` の指示に従って
   対象分野の `ROADMAP.md` を読み、前回の続きから対話形式で学習を進めます。
4. **セッションを終える** — 「今日はここまで」や `/wrapup` と伝えると、その回の学習内容が
   `ROADMAP.md` の進捗表・学習メモに反映され、commit・push まで自動で行われます。
   次回は 3. に戻るだけです。

## 6. うまくいかないときは

| 症状 | 原因と対処 |
|---|---|
| **Use this template** が見当たらない | そのリポジトリがテンプレートとして設定されていない。2-1 を参照 |
| 作ったリポジトリが Claude の **リポジトリを選択** に出てこない | Claude GitHub App のアクセス範囲に入っていない。[`MOBILE_SETUP.md`](./MOBILE_SETUP.md) の 3-5 で追加する |
| メニューに **コード** が見当たらない | サインインしているアカウントが Pro / Max プランか確認する |
| セッションの開始・終了まわりで困った | [`MOBILE_SETUP.md`](./MOBILE_SETUP.md) の 6 章「うまくいかないときは」を参照 |
